# Notas:

```
Options:
      --help     Show help                                              [boolean]
      --version  Show version number                                    [boolean]
  -b, --number   Es la base de la tabla de muntiplicar                  [number] [required]
  -l, --listar   Muestra la tabla en consola                            [boolean] [default: false]
  -h, --hasta    Indica hasta que numero va la tabla de multiplicar     [number] [default: 10]
